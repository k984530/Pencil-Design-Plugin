---
name: pencil-open-canvas
description: Pencil.dev 캔버스를 열어 디자인 작업을 시작합니다. 기존 .pen 파일을 열거나 새 캔버스를 생성합니다.
argument-hint: '".pen/screens/home.pen" (선택) — Pencil 캔버스 열기, 빈 값이면 새 캔버스 생성'
allowed-tools:
  - Read
  - Glob
  - Bash
  - mcp__pencil__*
---

# Open Canvas Command

Pencil.dev 캔버스를 여는 커맨드입니다.

## 사용 방법

### 새 캔버스 열기

```
/pencil-open-canvas
```

### 기존 파일 열기

```
/pencil-open-canvas .pen/screens/home.pen
```

### 컴포넌트 열기

```
/pencil-open-canvas .pen/components/button.pen
```

## 실행 단계

### Step 1: 인자 확인

- 인자 없음 → 새 캔버스 생성
- 파일 경로 → 해당 .pen 파일 열기

### Step 2: 파일 존재 확인 (경로 지정 시)

```bash
ls -la [file_path]
```

파일이 없으면 생성 여부 확인.

### Step 3: Pencil 캔버스 활성화

Pencil MCP를 통해 캔버스 열기:

```
Open pencil canvas
```

또는 특정 파일:

```
Open pencil canvas with .pen/screens/home.pen
```

### Step 4: 캔버스 상태 확인

캔버스가 정상적으로 열렸는지 확인하고 사용자에게 안내.

## .pen 파일 구조

프로젝트 내 .pen 파일 위치:

```
.pen/
├── screens/           # 화면 단위 디자인
│   ├── home.pen
│   ├── login.pen
│   └── dashboard.pen
└── components/        # 재사용 컴포넌트
    ├── button.pen
    ├── card.pen
    └── input.pen
```

## 예시

### 빈 캔버스 시작

```
/pencil-open-canvas
```

출력:
```
🎨 Pencil 캔버스 열기
━━━━━━━━━━━━━━━━━━━━

새 캔버스가 열렸습니다.

💡 팁:
- /pencil-design 명령으로 디자인 생성
- 디자인 시스템 참조: design/system/
- 저장 시 .pen/ 폴더에 저장됨
```

### 기존 디자인 열기

```
/pencil-open-canvas .pen/screens/login.pen
```

출력:
```
🎨 Pencil 캔버스 열기
━━━━━━━━━━━━━━━━━━━━

파일: .pen/screens/login.pen
상태: ✅ 열림

캔버스에서 login.pen 파일이 열렸습니다.
수정 후 저장하면 자동으로 Git에 반영됩니다.
```

### 프로젝트 내 .pen 파일 목록 보기

캔버스를 열기 전 어떤 파일들이 있는지 확인:

```
/pencil-open-canvas --list
```

출력:
```
📁 프로젝트 내 .pen 파일
━━━━━━━━━━━━━━━━━━━━

Screens (3):
  .pen/screens/home.pen
  .pen/screens/login.pen
  .pen/screens/dashboard.pen

Components (2):
  .pen/components/button.pen
  .pen/components/card.pen

열려면: /pencil-open-canvas [파일경로]
```

## Pencil.dev 요구사항

- Pencil.dev 앱이 설치되어 있어야 함 (Mac/Linux)
- Pencil MCP 서버가 실행 중이어야 함
- 플러그인의 `.mcp.json`에 pencil 서버 설정됨

## 문제 해결

### 캔버스가 열리지 않는 경우

1. Pencil.dev 앱 설치 확인
2. MCP 서버 상태 확인: `/mcp`
3. 터미널에서 수동 확인: `npx @pencil-dev/mcp`

### .pen 파일을 찾을 수 없는 경우

1. 경로가 정확한지 확인
2. `.pen/` 폴더 존재 확인
3. 새 파일 생성: `/pencil-design` 명령 사용

## 관련 명령어

- `/pencil-design` - 디자인 생성
- `/pencil-design-system` - 디자인 시스템 관리
